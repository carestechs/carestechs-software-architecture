# Module-specific FastAPI dependencies live here as the module grows
# (adrs/python/modular-packages.md). The catalog contract provider is injected
# by the composition root via create_router() — it is not imported here.
